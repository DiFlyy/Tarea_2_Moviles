<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="24dp"
    android:background="?attr/colorSurface">

    <!-- Switch para modo oscuro -->
    <androidx.appcompat.widget.SwitchCompat
        android:id="@+id/switchTheme"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Modo oscuro"
        android:textColor="?attr/colorOnSurface"
        android:layout_marginBottom="16dp" />

    <!-- Título -->
    <TextView
        android:id="@+id/tvAppTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🎵 Pop Music Explorer"
        android:textSize="24sp"
        android:textStyle="bold"
        android:textColor="?attr/colorOnSurface"
        android:layout_marginBottom="32dp" />

    <!-- Década 80s -->
    <LinearLayout
        android:orientation="vertical"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/btn80s"
            android:layout_width="220dp"
            android:layout_height="220dp"
            android:src="@drawable/ic_artist1"
            android:contentDescription="Década 80s"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/CircleShapeAppearance"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Década 80s"
            android:textColor="?attr/colorOnSurface"
            android:textSize="18sp"
            android:layout_marginTop="8dp" />
    </LinearLayout>

    <!-- Década 2010s -->
    <LinearLayout
        android:orientation="vertical"
        android:gravity="center"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content">

        <com.google.android.material.imageview.ShapeableImageView
            android:id="@+id/btn2010s"
            android:layout_width="220dp"
            android:layout_height="220dp"
            android:src="@drawable/ic_artist2"
            android:contentDescription="Década 2010s"
            android:scaleType="centerCrop"
            app:shapeAppearanceOverlay="@style/CircleShapeAppearance"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Década 2010s"
            android:textColor="?attr/colorOnSurface"
            android:textSize="18sp"
            android:layout_marginTop="8dp" />
    </LinearLayout>

</LinearLayout>
